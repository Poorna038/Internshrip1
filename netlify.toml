# netlify.toml — put at repo root
[build]
  command = "npm ci && npm run build"
  publish = "build"

[build.environment]
  REACT_APP_API_URL = "https://internshrip1.onrender.com"

# Optional: skip Netlify's asset processing if you want fewer automatic steps.
[build.processing]
  skip_processing = true
